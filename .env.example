# Vulcan Brownout Environment Configuration
#
# This file documents all environment variables used by the project.
# Copy this file to .env and fill in real values for your environment.
# .env is gitignored - NEVER commit it to the repository.
#
# Note: Component test mode (GitHub Actions) uses hardcoded constants
# and does NOT require this file. This is for integration and E2E testing only.

# ============================================================================
# Integration Test Mode (Manual / Scheduled)
# ============================================================================
# These values are used when running tests against a REAL Home Assistant instance.
# Copy this file to .env and fill in your actual HA credentials.

# Home Assistant Connection
HA_URL=http://homeassistant.lan
HA_PORT=8123
HA_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# HA Web Interface Login (for E2E / Playwright tests)
HA_USERNAME=admin
HA_PASSWORD=your_secure_password

# ============================================================================
# SSH / Deployment Credentials
# ============================================================================
# Required for deploying the integration to the test HA instance

SSH_HOST=homeassistant.lan
SSH_PORT=22
SSH_USER=root
SSH_KEY_PATH=.secrets/vulcan_deploy

# Path to HA config directory on the SSH host
HA_CONFIG_PATH=/root/homeassistant

# ============================================================================
# Local Docker Staging Environment
# ============================================================================
# Set these when running against the local Docker HA (development/environments/staging/)
# up.sh writes these automatically after onboarding.
#
# HA_URL=http://localhost:8123
# HA_TOKEN=<generated by up.sh>
# HA_USERNAME=admin
# HA_PASSWORD=<set in up.sh>
# SSH_HOST=  # Not needed for local Docker — integration is bind-mounted

# ============================================================================
# Component Test Mode (GitHub Actions / Docker)
# ============================================================================
# These are hardcoded in the component test workflow and do NOT use .env:
#   HA_URL=http://localhost:8123
#   HA_TOKEN=test-token-constant
#   HA_PORT=8123
#   TEST_MODE=component
#
# No GitHub Secrets needed for component tests.
